 写一个提交表单的页面，来提交saas环境的地址，名称，
再写一个页面来设置维护时间范围；
在同一个页面（状态页面）设置两个链接跳转去上面两个页面

数据提交后写到数据库里面；
每隔1分钟从数据库里面把saas环境的信息读出来（如果维护期还没有到，且当时服务正常就显示黄色图标，鼠标移动到图标上面显示维护时间；如果当时服务挂掉，显示红色图标，且显示维护时间），去请求数据根据返回状态码判断服务正常否；
返回状态存入redis里面，每次请求来从redis里面读取状态，连同页面一同返回到浏览器。


